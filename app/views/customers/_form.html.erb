<%= form_for(customer, remote: true) do |f| %>
  <% if customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer.errors.count, "error") %> prohibited this helpchat from being saved:</h2>

      <ul>
      <% customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
    <br />
    <%= f.label :membership_number %>
    <%= f.text_field :membership_number %>
    <br />
    <%= f.label "Description of Problem" %>
    <%= f.text_area :problem_description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
